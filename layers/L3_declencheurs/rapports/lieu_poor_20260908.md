# Mesure d'activation C2_POOR — verdict : v1 MORTE PAR CONSTRUCTION (08/09)

*Comptage de lieux seulement, aucun devenir lu. La mesure qui devait écrire
l'attendu a tué le câblage AVANT le premier jour de journal — c'est le
travail exact de la règle 15 : jamais un actif qui ne peut pas déclencher.*

## 1. Mesure d'activation (v1 : état 15 min dernière-minute, épisode ≥ 3 barres, retour ≤ P10)

| | jours évalués | lieux | signaux |
|---|---|---|---|
| ES | 52 | **0** | 0 |
| NQ | 52 | **0** | 0 |

## 2. Diagnostic — pourquoi zéro

Persistance de `ctx_poor_high` à l'agrégat 15 min (dernière minute de
fenêtre), 5 jours × 2 instruments :

| | barres vraies | épisodes ≥ 4 barres consécutives |
|---|---|---|
| ES | 6/104 | **0** |
| NQ | 5/104 | **0** |

L'exigence « formé depuis ≥ 3 barres » est INSATISFIABLE à ce grain :

1. le flag C++ est un détecteur ROULANT 60 min (`excess_high_bars < 3`),
   pas une mémoire d'épisode — il s'allume et s'éteint au fil du prix ;
2. le RETOUR du prix vers le niveau — la condition même du lieu — remplit
   `excess` et ÉTEINT le flag avant la clôture de la fenêtre 15 min : le
   lieu et l'état sont quasi mutuellement exclusifs à la dernière minute.

La reproduction, elle, est prouvée (`test_ctx`, mismatch = 0 sur 1 259
barres stables) : la colonne est juste, c'est la SÉMANTIQUE d'épisode qui
manque.

## 3. Décision

- **C2_POOR ne s'active PAS** : un setup structurellement muet déclaré
  actif fabriquerait « testé, N = 0 = rareté » — le mensonge exact que la
  règle 15 interdit.
- Le PRÉ-CÂBLAGE est conservé et commité : fonction `c2_poor`, huit cas
  verts (activation le temps du test), `rvol_min` mesuré (p50 0,991 ES /
  0,979 NQ, attendu tenu), portage `ctx_poor_*` dans l'agrégation.
- **Redesign J+2, brief Fable** : une MÉMOIRE D'ÉPISODE — un poor détecté
  au plus haut du jour RESTE poor jusqu'à réparation (clôture au-delà) ou
  invalidation, la sémantique Dalton — le flag roulant ne sert qu'à DATER
  la formation, plus à porter l'état. Le brief doit AUSSI écarter par
  mesure l'option « portage `.max()` de fenêtre » (persistance différente
  à coût quasi nul), pas par omission (review, suggestion 4) — et trancher
  le NaN d'épisode (tenir ou clore).
